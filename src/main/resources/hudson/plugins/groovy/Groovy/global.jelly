<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:section title="${%Groovy}">
    <f:entry title="${%Allow token macro processing}" description="${%Possible security hole, read help!}" field="allowMacro">
    	<f:checkbox checked="${descriptor.allowMacro}" />
    </f:entry>
  </f:section>
</j:jelly>
